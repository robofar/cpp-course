#include <iostream>
#include <fstream>

int main ()
{
    std::iostream ulazni_tok("TEMPERATURE.TXT");
    std::ostream izlazni_tok("IZVJESTAJ.TXT");
    while(ulazni_tok || !ulazni_tok.eof()) {
        
    }
	return 0;
}